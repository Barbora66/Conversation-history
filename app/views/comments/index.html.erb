<% content_for :title, "Comments" %>

<div class="w-full">

  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Comments</h1>
    <%= link_to "New comment", new_comment_path, class: "rounded-md px-3.5 py-2.5 bg-violet-500 hover:bg-violet-700 text-white block font-medium" %>
  </div>

  <div id="comments" class="min-w-full">
    <% if @comments.any? %>
      <% @comments.each do |comment| %>
        <%= render comment %>
        <p>
          <%= link_to "Show this comment", comment, class: "ml-2 rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 inline-block font-medium" %>
        </p>
      <% end %>
    <% else %>
      <p class="text-center my-10">No comments found.</p>
    <% end %>
  </div>
</div>
